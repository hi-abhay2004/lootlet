// FilterSidebar component for category filters and sorting
export default function FilterSidebar({ filters, onChange }) {
  return (
    <aside className="w-full md:w-64 bg-gray-50 p-4 rounded mb-4 md:mb-0">
      <h3 className="font-bold mb-2">Filters</h3>
      {/* TODO: Add filter controls */}
    </aside>
  );
}
